/*----------Slice to return a part of an array-----------
var  arr = ["a", "b", "c", "d"];

const sliceArr = (array, begin, end) => array.slice(begin, end);

console.log(sliceArr(arr, 0, 2));//return [ 'a', 'b' ]
/*----------Slice to return a part of an array-----------*/
